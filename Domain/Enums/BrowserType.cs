namespace Domain.Enums;

/// <summary>
/// Holds all available browser supported by playwright. Used in appsettings to decide browser used by robot. 
/// </summary>
public enum BrowserType
{
    Chrome,
    Firefox,
    Webkit
}